framework:
  rate_limiter:
    extension_api:
      policy: fixed_window
      limit: 60
      interval: '1 minute'
    auth_login:
      policy: sliding_window
      limit: 8
      interval: '15 minutes'
    auth_register:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    password_reset:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    contact_form:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    share_invitation:
      policy: sliding_window
      limit: 20
      interval: '1 hour'
