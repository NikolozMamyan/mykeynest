framework:
  rate_limiter:
    extension_api:
      policy: fixed_window
      limit: 60
      interval: '1 minute'
    extension_pairing:
      policy: sliding_window
      limit: 12
      interval: '10 minutes'
    auth_login:
      policy: sliding_window
      limit: 8
      interval: '15 minutes'
    auth_register:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    password_reset:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    contact_form:
      policy: sliding_window
      limit: 5
      interval: '1 hour'
    support_chat:
      policy: sliding_window
      limit: 30
      interval: '1 hour'
    share_invitation:
      policy: sliding_window
      limit: 20
      interval: '1 hour'
