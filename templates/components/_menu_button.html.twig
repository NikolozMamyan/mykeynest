{# components/_menu_button.html.twig #}

<style>

/* menu-button.css ou dans votre fichier CSS principal */
.menu-button {
    display: inline-block;
    font-family: 'Press Start 2P', cursive;
    font-size: 14px;
    padding: 12px 24px;
    margin: 20px 0;
    background: transparent;
    color: var(--accent-color);
    border: 2px solid var(--accent-color);
    border-radius: 4px;
    text-decoration: none;
    text-align: center;
    transition: all 0.3s;
}

.menu-button:hover {
    background: var(--accent-color);
    color: var(--dark-bg);
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(255,156,0,0.3);
}

.menu-button::before {
    content: "◀ ";
}

</style>
<a href="{{ path('app_menu') }}" class="menu-button">{{ button_text }}</a>