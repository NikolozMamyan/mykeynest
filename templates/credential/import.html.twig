{% extends 'base.html.twig' %}

{% block body %}
<div class="import-container">
  <div class="import-header">
    <h1>{{ heading }}</h1>
    <p class="import-subtitle">Importez vos identifiants en masse depuis un fichier CSV</p>
  </div>

  <div class="import-content">
    {# Section exemple CSV #}
    <div class="csv-example-card">
      <div class="csv-example-header">
        <svg class="icon-info" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="12" y1="16" x2="12" y2="12"></line>
          <line x1="12" y1="8" x2="12.01" y2="8"></line>
        </svg>
        <h3>Format CSV attendu</h3>
      </div>
      
      <div class="csv-example-content">
        <p class="csv-info">Le fichier doit contenir les colonnes suivantes (séparateur <code>,</code> ou <code>;</code>) :</p>
        
        <div class="csv-code-block">
          <pre id="csvExample">name,domain,username,password
Mon compte Gmail,gmail.com,user@gmail.com,motdepasse123
Netflix,netflix.com,john.doe@email.com,MyP@ssw0rd!</pre>
          <button type="button" class="btn-copy" onclick="copyCsvExample()">
            <svg class="icon-copy" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
              <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
            </svg>
            <span class="copy-text">Copier</span>
          </button>
        </div>

        <div class="csv-notes">
          <span class="note-item">✓ Les 4 colonnes sont obligatoires</span>
          <span class="note-item">✓ L'en-tête doit être présent</span>
          <span class="note-item">✓ Encodage UTF-8 recommandé</span>
        </div>
      </div>
    </div>

    {# Formulaire d'upload #}
    <div class="upload-card">
      <form method="post" enctype="multipart/form-data">
        <div class="file-upload-wrapper">
          <label for="csv_file" class="file-upload-label">
            <svg class="icon-upload" xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="17 8 12 3 7 8"></polyline>
              <line x1="12" y1="3" x2="12" y2="15"></line>
            </svg>
            <span class="upload-text">Choisir un fichier CSV</span>
            <span class="upload-hint">ou glissez-déposez le fichier ici</span>
            <span class="file-name" id="fileName"></span>
          </label>
          <input 
            type="file" 
            name="csv_file" 
            id="csv_file" 
            accept=".csv,text/csv" 
            required
            onchange="updateFileName(this)"
          >
        </div>

        <button type="submit" class="btn-import">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
          Importer les identifiants
        </button>
      </form>
    </div>
  </div>
</div>

<style>
.import-container {
  max-width: 800px;
  margin: 2rem auto;
  padding: 0 1.5rem;
}

.import-header {
  text-align: center;
  margin-bottom: 3rem;
}

.import-header h1 {
  color: var(--color-text-dark);
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.import-subtitle {
  color: var(--color-text-medium);
  font-size: 1rem;
}

.import-content {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

/* CSV Example Card */
.csv-example-card {
  background: var(--color-white);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-sm);
  overflow: hidden;
}

.csv-example-header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1.25rem 1.5rem;
  background: var(--color-bg-light);
  border-bottom: 1px solid var(--color-border);
}

.csv-example-header h3 {
  margin: 0;
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--color-text-dark);
}

.icon-info {
  color: var(--color-primary);
  flex-shrink: 0;
}

.csv-example-content {
  padding: 1.5rem;
}

.csv-info {
  color: var(--color-text-medium);
  margin-bottom: 1rem;
  font-size: 0.95rem;
}

.csv-info code {
  background: var(--color-bg-light);
  padding: 0.2rem 0.5rem;
  border-radius: 4px;
  font-family: 'Courier New', monospace;
  font-size: 0.9em;
  color: var(--color-primary);
}

.csv-code-block {
  position: relative;
  background: var(--color-text-dark);
  border-radius: var(--border-radius-sm);
  padding: 1.25rem;
  margin-bottom: 1rem;
}

.csv-code-block pre {
  margin: 0;
  color: #10b981;
  font-family: 'Courier New', monospace;
  font-size: 0.875rem;
  line-height: 1.6;
  overflow-x: auto;
}

.btn-copy {
  position: absolute;
  top: 0.75rem;
  right: 0.75rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: var(--color-white);
  padding: 0.5rem 0.875rem;
  border-radius: var(--border-radius-sm);
  cursor: pointer;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.2s;
}

.btn-copy:hover {
  background: rgba(255, 255, 255, 0.2);
  transform: translateY(-1px);
}

.btn-copy.copied {
  background: var(--color-success);
  border-color: var(--color-success);
}

.icon-copy {
  flex-shrink: 0;
}

.csv-notes {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.note-item {
  display: flex;
  align-items: center;
  color: var(--color-text-medium);
  font-size: 0.875rem;
}

/* Upload Card */
.upload-card {
  background: var(--color-white);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-sm);
  padding: 2rem;
}

.file-upload-wrapper {
  position: relative;
  margin-bottom: 1.5rem;
}

.file-upload-label {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 3rem 2rem;
  border: 2px dashed var(--color-border);
  border-radius: var(--border-radius);
  background: var(--color-bg-light);
  cursor: pointer;
  transition: all 0.3s;
}

.file-upload-label:hover {
  border-color: var(--color-primary);
  background: rgba(5, 150, 105, 0.05);
}

.file-upload-wrapper input[type="file"] {
  position: absolute;
  width: 1px;
  height: 1px;
  opacity: 0;
  overflow: hidden;
}

.icon-upload {
  color: var(--color-primary);
  margin-bottom: 1rem;
}

.upload-text {
  color: var(--color-text-dark);
  font-weight: 600;
  font-size: 1.125rem;
  margin-bottom: 0.5rem;
}

.upload-hint {
  color: var(--color-text-muted);
  font-size: 0.875rem;
}

.file-name {
  display: none;
  margin-top: 1rem;
  padding: 0.75rem 1.25rem;
  background: var(--color-white);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius-sm);
  color: var(--color-primary);
  font-weight: 500;
  font-size: 0.9rem;
}

.file-name.active {
  display: block;
}

.btn-import {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
  background: var(--color-primary);
  color: var(--color-white);
  border: none;
  padding: 1rem 2rem;
  border-radius: var(--border-radius-sm);
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  box-shadow: var(--shadow-md);
}

.btn-import:hover {
  background: #047857;
  box-shadow: var(--shadow-green);
  transform: translateY(-2px);
}

.btn-import:active {
  transform: translateY(0);
}

@media (max-width: 640px) {
  .import-header h1 {
    font-size: 1.5rem;
  }
  
  .csv-example-content {
    padding: 1rem;
  }
  
  .upload-card {
    padding: 1.5rem;
  }
  
  .file-upload-label {
    padding: 2rem 1rem;
  }
}
</style>

<script>
function copyCsvExample() {
  const text = document.getElementById('csvExample').textContent;
  const button = event.currentTarget;
  const copyText = button.querySelector('.copy-text');
  
  navigator.clipboard.writeText(text).then(() => {
    button.classList.add('copied');
    copyText.textContent = 'Copié !';
    
    setTimeout(() => {
      button.classList.remove('copied');
      copyText.textContent = 'Copier';
    }, 2000);
  });
}

function updateFileName(input) {
  const fileNameSpan = document.getElementById('fileName');
  if (input.files && input.files[0]) {
    fileNameSpan.textContent = input.files[0].name;
    fileNameSpan.classList.add('active');
  } else {
    fileNameSpan.classList.remove('active');
  }
}
</script>
{% endblock %}