<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Identifiants partagés avec vous</title>
</head>
<body style="margin: 0; padding: 0; background-color: #f8fafc; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;">

  <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background-color: #f8fafc;">
    <tr>
      <td align="center" style="padding: 48px 16px;">

        <table role="presentation" width="100%" style="max-width: 540px;">

          <!-- Logo -->
          <tr>
            <td align="center" style="padding-bottom: 28px;">
              {% include 'components/parts/logo.html.twig' %}
            </td>
          </tr>

          <!-- Card -->
          <tr>
            <td style="background: #ffffff; border-radius: 12px; border: 1px solid #e2e8f0; box-shadow: 0 4px 16px rgba(0,0,0,0.05); overflow: hidden;">

              <!-- Top accent -->
              <table role="presentation" width="100%" cellspacing="0" cellpadding="0">
                <tr>
                  <td style="background: #059669; height: 3px; font-size: 0; line-height: 0;">&nbsp;</td>
                </tr>
              </table>

              <!-- Content -->
              <table role="presentation" width="100%" cellspacing="0" cellpadding="0">
                <tr>
                  <td style="padding: 40px 40px 36px;">

                    <!-- Eyebrow -->
                    <p style="margin: 0 0 18px; font-size: 11px; font-weight: 700; color: #059669; text-transform: uppercase; letter-spacing: 0.08em;">
                      Nouveau partage
                    </p>

                    <!-- Title -->
                    <h1 style="margin: 0 0 12px; font-size: 22px; font-weight: 700; color: #1e293b; line-height: 1.3; letter-spacing: -0.3px;">
                      {{ owner.email }} a partagé {{ credentials|length }} identifiant{{ credentials|length > 1 ? 's' : '' }} avec vous
                    </h1>

                    <!-- Intro -->
                    <p style="margin: 0 0 28px; font-size: 15px; color: #64748b; line-height: 1.6;">
                      Vous pouvez dès maintenant accéder à {{ credentials|length > 1 ? 'ces identifiants' : 'cet identifiant' }} depuis votre espace MYKEYNEST.
                    </p>

                    <!-- Divider -->
                    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="margin-bottom: 24px;">
                      <tr>
                        <td style="border-top: 1px solid #f1f5f9; font-size: 0; line-height: 0;">&nbsp;</td>
                      </tr>
                    </table>

                    <!-- Credentials list -->
                    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="margin-bottom: 28px;">
                      {% for credential in credentials %}
                        <tr>
                          <td style="padding: {{ loop.first ? '0' : '10px' }} 0 10px; {{ loop.last ? '' : 'border-bottom: 1px solid #f1f5f9;' }}">
                            <table role="presentation" width="100%" cellspacing="0" cellpadding="0">
                              <tr>
                                <td style="width: 36px; vertical-align: middle;">
                                  <div style="width: 32px; height: 32px; background: linear-gradient(135deg, #059669, #047857); border-radius: 8px; text-align: center; line-height: 32px; font-size: 11px; font-weight: 700; color: #ffffff;">
                                    {{ credential.name|slice(0,2)|upper }}
                                  </div>
                                </td>
                                <td style="padding-left: 12px; vertical-align: middle;">
                                  <p style="margin: 0 0 2px; font-size: 14px; font-weight: 600; color: #1e293b;">{{ credential.name }}</p>
                                  <p style="margin: 0; font-size: 12px; color: #94a3b8;">{{ credential.domain }} · {{ credential.username }}</p>
                                </td>
                              </tr>
                            </table>
                          </td>
                        </tr>
                      {% endfor %}
                    </table>

                    <!-- CTA -->
                    <table role="presentation" cellspacing="0" cellpadding="0">
                      <tr>
                        <td style="border-radius: 8px; background: #059669;">
                          <a href="{{ app_url }}"
                             style="display: inline-block; padding: 13px 28px; font-size: 14px; font-weight: 600; color: #ffffff; text-decoration: none; border-radius: 8px; letter-spacing: 0.1px;">
                            Accéder à mes partages
                          </a>
                        </td>
                      </tr>
                    </table>

                  </td>
                </tr>
              </table>

            </td>
          </tr>

          <!-- Footer -->
          <tr>
            <td align="center" style="padding: 28px 16px 0;">
              <p style="margin: 0 0 6px; font-size: 12px; color: #94a3b8; line-height: 1.6;">
                Vous recevez cet email car un utilisateur MYKEYNEST a partagé des identifiants avec vous.
              </p>
              <p style="margin: 0; font-size: 12px; color: #cbd5e1;">
                Si vous pensez avoir reçu ce message par erreur, contactez-nous.
              </p>
            </td>
          </tr>

        </table>
      </td>
    </tr>
  </table>

</body>
</html>
