{{ 'reset_password.email.text_intro'|trans }}

{{ 'reset_password.email.text_action'|trans }}
{{ url('app_reset_password', {token: resetToken.token}) }}

{{ 'reset_password.email.expiry'|trans({ '%duration%': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }) }}

{{ 'reset_password.email.text_ignore'|trans }}

MYKEYNEST
